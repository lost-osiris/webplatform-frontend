import NotFound from '../components/Views/NotFound'
import * as UI from '../components/UI'

var route = {
  route: {
  },
  ui: {
    stateTitle: UI.StateTitle.NotFound,
    content: NotFound,
  }
}

export default route
